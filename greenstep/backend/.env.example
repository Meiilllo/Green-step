PORT=3000
NODE_ENV=production
STORAGE_DIR=/app/backend/storage

# If frontend and backend are on different domains, specify allowed origins:
# CORS_ORIGIN=https://example.com,https://www.example.com
CORS_ORIGIN=
